import type { NextConfig } from "next";

const nextConfig: NextConfig = {
   /* config options here */
   images: {
      domains: [
         "images.unsplash.com",
         "source.unsplash.com",
         "api.pexels.com",
         "storage.eventyay.com",
      ],
   },
};

export default nextConfig;
